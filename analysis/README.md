# Analysis Notes

`example_key_contacts.csv` is an optional manually curated example for the TS3a/TS3b case.

It is not generated by `run_workflow.py`; the main automated workflow produces Multiwfn cube/log files and VMD/Tachyon clean figures.