Place local Gaussian formatted checkpoint files here before running the workflow.

Default expected filenames:

```text
TS3a_SP_PCM.fchk
TS3b_SP_PCM.fchk
```

The `.fchk` files are ignored by Git because they are usually large and may
contain unpublished quantum-chemical data.
